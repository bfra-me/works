---
'@bfra.me/semantic-release': patch
---

Fix `PluginConfig` and `Plugin` type resolution under `skipLibCheck: false`, and export `GlobalConfig`, `BranchSpec`, and `PluginSpec` from the package root.
